extends ../layout

block content
  table(style="width: 100%")
    - each user in users
      tr
        td
          a(href='/users/'+user._id)= user.name
        td
          a(href='/users/'+user._id)= user.displayname 
        td
          span= user.role
        td
          a(href='/users/remove/'+user._id) (löschen)
