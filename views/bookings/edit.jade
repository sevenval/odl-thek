form(method='post',action="/bookings/"+booking.gadget+"/edit",id="book")
.getBook
    h1(onclick="$('#layerBookGadget').hide();$('#blank').hide();") Update Booking
    span.bookBrand= booking.gadgetname
    a(onclick="$('#book').hide();") X
  if(locals.error)
    .error= error
  .bookingTime
    .start
      h2 Start
      input(type='date', name='startdate', value=startdate)
      span.dateIcon
      br
      input(type='time', name='starttime', value=starttime)
      span.timeIcon
    .btw
    .end
      h2 End
      input(type='date', name='enddate', value=enddate)
      span.dateIcon
      input(type='time', name='endtime', value=endtime)
      span.timeIcon
    .clear
    div(onclick="bookNow(); return false;").book
      span Speichern
  br
